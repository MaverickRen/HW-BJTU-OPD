# Frozen public inference/evaluation environment. Install this through
# scripts/install_eval.sh: vLLM 0.18 declares transformers<5 even though the
# successful Qwen3.5 runtime uses Transformers 5.5, so the installer performs
# the audited final override after resolving vLLM's remaining dependencies.
torch==2.10.0
torchvision==0.25.0
nvidia-nccl-cu12==2.27.5
vllm==0.18.0
flashinfer-python==0.6.6
# quack-kernels 0.5.0 (pulled by vLLM) still uses cutlass.cute.core.ThrMma.
# CUTLASS DSL 4.6+ removed that public symbol, so do not leave this transitive
# dependency open-ended.
quack-kernels==0.5.0
nvidia-cutlass-dsl==4.5.3
Pillow==12.0.0
pyarrow==22.0.0
openai==2.14.0
qwen-vl-utils==0.0.14
tqdm==4.67.1
typer==0.25.1
